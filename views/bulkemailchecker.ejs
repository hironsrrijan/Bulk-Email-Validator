<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hiran Roy Bulk Email Validator</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  </head>
  <body>
    <br />
    <br />
    <div class="container">
      <h1 class="text-center"> Hiran Roy Bulk Email Validator </h1>
      <form action="/bulkemailchecker" method="post">
        <div class="form-group">
          <label for="emails" style="font-weight:bolder;">Enter Bulk Emails Here:</label>
          <textarea class="form-control" cols="20" rows="10" name="domain" placeholder="Enter Emails" id="inputtextarea" required></textarea>
        </div>
        <div class="form-group">
          <br>
          <label for="provider" style="font-weight:bolder;">Select Email Provider:</label>
          <select class="form-control" name="provider">
            <option value="gmail.com" selected>Gmail</option>
            <option value="yahoo.com">Yahoo</option>
            <option value="outlook.com">Outlook+Hotmail</option>
          </select>
        </div>
        <br>
        <div class="form-group">
          <button id="button" class="btn btn-danger col-12"> Verify Emails </button>
        </div>
        <br>
        <table class="table table-striped">
          <thead>
            <tr>
              <th>Verified Emails</th>
            </tr>
          </thead>
          <tbody><%if(flag){%><%for(var i=0; i
													<text.verified.length; i++) {%> <tr>
              <td><%=text.verified[i]%>
              <td>
            </tr><%}%><%}%> </tbody>
        </table>
        <table class="table table-striped">
          <thead>
            <tr>
              <th>UnVerified Emails</th>
            </tr>
          </thead>
          <tbody><%if(flag){%><%for(var i=0; i
																<text.unverified.length; i++) {%> <tr>
              <td><%=text.unverified[i]%> </td>
            </tr><%}%><%}%> </tbody>
        </table>
    </div>
  </body>
</html>